# Internationalization (i18n) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add runtime language switching (English, Vietnamese, Chinese, Japanese) via `ngx-translate`, an About dialog with language selector (issue #13), and remove the hardcoded "S" prefix from the session chip.

**Architecture:** `ngx-translate` loads JSON translation files from `public/i18n/{lang}.json` at runtime. A `LanguageService` detects browser locale on startup, applies a persisted `localStorage` preference, and exposes `setLanguage()`. All UI strings are replaced with the `translate` pipe. An `AboutDialog` opened by a toolbar ℹ️ icon contains app version, credits, and the language selector.

**Tech Stack:** Angular 20 standalone components, `@ngx-translate/core`, `@ngx-translate/http-loader`, Angular Material `MatDialog`, `MatSelect`.

**Spec:** `docs/superpowers/specs/2026-05-18-i18n-design.md`

---

### Task 1: Install `ngx-translate` and create translation JSON files

**Files:**
- Modify: `package.json`
- Create: `public/i18n/en.json`
- Create: `public/i18n/vi.json`
- Create: `public/i18n/zh.json`
- Create: `public/i18n/ja.json`

- [ ] **Step 1: Install packages**

```bash
npm install @ngx-translate/core @ngx-translate/http-loader
```

Expected: packages appear in `node_modules/`, no errors

- [ ] **Step 2: Create `public/i18n/en.json`**

```json
{
  "toolbar": {
    "app_title": "Pickleball Round Robin"
  },
  "players": {
    "title": "Players",
    "add_placeholder": "Player name",
    "add_button": "Add",
    "empty_state": "Add 8–11 players to get started.",
    "add_more": "Add {{ count }} more player(s) to generate schedule",
    "delete_tooltip": "Cannot remove players after the schedule has been generated. Use Regenerate Schedule on the Schedule tab to start over.",
    "roster_locked_snack": "Roster is locked. Regenerate the schedule first.",
    "generate_button": "Generate Schedule ({{ count }} players · 2 courts)"
  },
  "schedule": {
    "title": "Schedule",
    "empty_state": "Add 8–11 players on the Players tab and generate a schedule.",
    "round": "Round {{ number }}",
    "sitting_out": "Sitting out: {{ names }}",
    "regenerate_button": "Regenerate Schedule"
  },
  "scores": {
    "title": "Scores",
    "empty_state": "Generate a schedule first."
  },
  "leaderboard": {
    "title": "Leaderboard",
    "no_scores": "No scores recorded yet.",
    "sort_wins": "Wins",
    "sort_points": "Points",
    "stat_line": "{{ wins }} wins · {{ points }} pts · {{ games }} games",
    "share_button": "Share QR",
    "reset_button": "Reset Session",
    "shared_readonly": "Shared session — read only",
    "view_only": "View Only"
  },
  "session_drawer": {
    "title": "Switch Session",
    "date_label": "Date",
    "new_session": "+ New Session",
    "active_label": "active",
    "empty_state": "No sessions for this date yet",
    "delete_confirm_title": "Delete Session {{ number }}?",
    "delete_confirm_message": "All players, rounds, and scores will be permanently removed."
  },
  "confirm_dialog": {
    "cancel": "Cancel",
    "delete": "Delete",
    "reset": "Reset",
    "regenerate": "Regenerate",
    "regenerate_title": "Regenerate Schedule?",
    "regenerate_message": "All rounds and scores will be cleared. Your player list will be kept.",
    "reset_title": "Reset Session?",
    "reset_message": "All players, rounds, and scores will be cleared."
  },
  "about": {
    "title": "About",
    "version": "Version {{ version }}",
    "language_label": "Language",
    "close": "Close"
  }
}
```

- [ ] **Step 3: Create `public/i18n/vi.json`**

```json
{
  "toolbar": {
    "app_title": "Vòng Robin Pickleball"
  },
  "players": {
    "title": "Người chơi",
    "add_placeholder": "Tên người chơi",
    "add_button": "Thêm",
    "empty_state": "Thêm 8–11 người chơi để bắt đầu.",
    "add_more": "Thêm {{ count }} người chơi nữa để tạo lịch",
    "delete_tooltip": "Không thể xóa người chơi sau khi lịch đã được tạo. Dùng Tạo lại lịch trên tab Lịch để bắt đầu lại.",
    "roster_locked_snack": "Danh sách đã khóa. Tạo lại lịch trước.",
    "generate_button": "Tạo lịch ({{ count }} người · 2 sân)"
  },
  "schedule": {
    "title": "Lịch thi đấu",
    "empty_state": "Thêm 8–11 người chơi ở tab Người chơi và tạo lịch.",
    "round": "Vòng {{ number }}",
    "sitting_out": "Nghỉ: {{ names }}",
    "regenerate_button": "Tạo lại lịch"
  },
  "scores": {
    "title": "Điểm số",
    "empty_state": "Tạo lịch trước."
  },
  "leaderboard": {
    "title": "Bảng xếp hạng",
    "no_scores": "Chưa có điểm nào.",
    "sort_wins": "Thắng",
    "sort_points": "Điểm",
    "stat_line": "{{ wins }} thắng · {{ points }} điểm · {{ games }} trận",
    "share_button": "Chia sẻ QR",
    "reset_button": "Đặt lại phiên",
    "shared_readonly": "Phiên được chia sẻ — chỉ xem",
    "view_only": "Chỉ xem"
  },
  "session_drawer": {
    "title": "Chuyển phiên",
    "date_label": "Ngày",
    "new_session": "+ Phiên mới",
    "active_label": "đang hoạt động",
    "empty_state": "Chưa có phiên nào cho ngày này",
    "delete_confirm_title": "Xóa Phiên {{ number }}?",
    "delete_confirm_message": "Tất cả người chơi, vòng đấu và điểm số sẽ bị xóa vĩnh viễn."
  },
  "confirm_dialog": {
    "cancel": "Hủy",
    "delete": "Xóa",
    "reset": "Đặt lại",
    "regenerate": "Tạo lại",
    "regenerate_title": "Tạo lại lịch?",
    "regenerate_message": "Tất cả vòng đấu và điểm số sẽ bị xóa. Danh sách người chơi sẽ được giữ lại.",
    "reset_title": "Đặt lại phiên?",
    "reset_message": "Tất cả người chơi, vòng đấu và điểm số sẽ bị xóa."
  },
  "about": {
    "title": "Giới thiệu",
    "version": "Phiên bản {{ version }}",
    "language_label": "Ngôn ngữ",
    "close": "Đóng"
  }
}
```

- [ ] **Step 4: Create `public/i18n/zh.json`**

```json
{
  "toolbar": {
    "app_title": "匹克球循环赛"
  },
  "players": {
    "title": "球员",
    "add_placeholder": "球员姓名",
    "add_button": "添加",
    "empty_state": "请添加 8–11 名球员以开始。",
    "add_more": "还需添加 {{ count }} 名球员才能生成赛程",
    "delete_tooltip": "生成赛程后无法删除球员。请使用"重新生成赛程"功能重新开始。",
    "roster_locked_snack": "名单已锁定，请先重新生成赛程。",
    "generate_button": "生成赛程（{{ count }} 名球员 · 2 块场地）"
  },
  "schedule": {
    "title": "赛程",
    "empty_state": "请在"球员"选项卡中添加 8–11 名球员并生成赛程。",
    "round": "第 {{ number }} 轮",
    "sitting_out": "本轮休息：{{ names }}",
    "regenerate_button": "重新生成赛程"
  },
  "scores": {
    "title": "比分",
    "empty_state": "请先生成赛程。"
  },
  "leaderboard": {
    "title": "排行榜",
    "no_scores": "尚未记录任何比分。",
    "sort_wins": "胜场",
    "sort_points": "积分",
    "stat_line": "{{ wins }} 胜 · {{ points }} 分 · {{ games }} 场",
    "share_button": "分享二维码",
    "reset_button": "重置本场",
    "shared_readonly": "共享场次 — 只读",
    "view_only": "只读"
  },
  "session_drawer": {
    "title": "切换场次",
    "date_label": "日期",
    "new_session": "+ 新建场次",
    "active_label": "当前",
    "empty_state": "该日期暂无场次",
    "delete_confirm_title": "删除第 {{ number }} 场？",
    "delete_confirm_message": "所有球员、轮次和比分将被永久删除。"
  },
  "confirm_dialog": {
    "cancel": "取消",
    "delete": "删除",
    "reset": "重置",
    "regenerate": "重新生成",
    "regenerate_title": "重新生成赛程？",
    "regenerate_message": "所有轮次和比分将被清除，球员名单将保留。",
    "reset_title": "重置本场？",
    "reset_message": "所有球员、轮次和比分将被清除。"
  },
  "about": {
    "title": "关于",
    "version": "版本 {{ version }}",
    "language_label": "语言",
    "close": "关闭"
  }
}
```

- [ ] **Step 5: Create `public/i18n/ja.json`**

```json
{
  "toolbar": {
    "app_title": "ピックルボール総当たり"
  },
  "players": {
    "title": "プレイヤー",
    "add_placeholder": "プレイヤー名",
    "add_button": "追加",
    "empty_state": "8〜11人のプレイヤーを追加して始めましょう。",
    "add_more": "スケジュールを生成するにはあと {{ count }} 人追加してください",
    "delete_tooltip": "スケジュール生成後はプレイヤーを削除できません。「スケジュール再生成」を使ってやり直してください。",
    "roster_locked_snack": "名簿はロックされています。先にスケジュールを再生成してください。",
    "generate_button": "スケジュール生成（{{ count }} 人 · コート2面）"
  },
  "schedule": {
    "title": "スケジュール",
    "empty_state": "プレイヤータブで8〜11人を追加してスケジュールを生成してください。",
    "round": "ラウンド {{ number }}",
    "sitting_out": "休憩: {{ names }}",
    "regenerate_button": "スケジュール再生成"
  },
  "scores": {
    "title": "スコア",
    "empty_state": "先にスケジュールを生成してください。"
  },
  "leaderboard": {
    "title": "リーダーボード",
    "no_scores": "まだスコアが記録されていません。",
    "sort_wins": "勝利数",
    "sort_points": "ポイント",
    "stat_line": "{{ wins }} 勝 · {{ points }} pt · {{ games }} 試合",
    "share_button": "QR共有",
    "reset_button": "セッションリセット",
    "shared_readonly": "共有セッション — 読み取り専用",
    "view_only": "読み取り専用"
  },
  "session_drawer": {
    "title": "セッション切替",
    "date_label": "日付",
    "new_session": "+ 新規セッション",
    "active_label": "アクティブ",
    "empty_state": "この日付のセッションはありません",
    "delete_confirm_title": "セッション {{ number }} を削除しますか？",
    "delete_confirm_message": "すべてのプレイヤー、ラウンド、スコアが完全に削除されます。"
  },
  "confirm_dialog": {
    "cancel": "キャンセル",
    "delete": "削除",
    "reset": "リセット",
    "regenerate": "再生成",
    "regenerate_title": "スケジュールを再生成しますか？",
    "regenerate_message": "すべてのラウンドとスコアが削除されます。プレイヤーリストは保持されます。",
    "reset_title": "セッションをリセットしますか？",
    "reset_message": "すべてのプレイヤー、ラウンド、スコアが削除されます。"
  },
  "about": {
    "title": "バージョン情報",
    "version": "バージョン {{ version }}",
    "language_label": "言語",
    "close": "閉じる"
  }
}
```

- [ ] **Step 6: Verify build compiles**

```bash
npx ng build --configuration=development 2>&1 | tail -5
```

Expected: build succeeds

- [ ] **Step 7: Commit**

```bash
git add public/i18n/ package.json package-lock.json
git commit -m "feat(i18n): install ngx-translate and add en/vi/zh/ja translation files"
```

---

### Task 2: Configure `TranslateModule` in `app.config.ts`

**Files:**
- Modify: `src/app/app.config.ts`

- [ ] **Step 1: Update `src/app/app.config.ts`**

Replace the full file:

```ts
import { ApplicationConfig, provideBrowserGlobalErrorListeners, provideZoneChangeDetection } from '@angular/core';
import { provideHttpClient } from '@angular/common/http';
import { importProvidersFrom } from '@angular/core';
import { TranslateModule, TranslateLoader } from '@ngx-translate/core';
import { TranslateHttpLoader } from '@ngx-translate/http-loader';
import { HttpClient } from '@angular/common/http';

export function createTranslateLoader(http: HttpClient): TranslateHttpLoader {
  return new TranslateHttpLoader(http, '/i18n/', '.json');
}

export const appConfig: ApplicationConfig = {
  providers: [
    provideBrowserGlobalErrorListeners(),
    provideZoneChangeDetection({ eventCoalescing: true }),
    provideHttpClient(),
    importProvidersFrom(
      TranslateModule.forRoot({
        loader: {
          provide: TranslateLoader,
          useFactory: createTranslateLoader,
          deps: [HttpClient],
        },
        defaultLanguage: 'en',
      })
    ),
  ],
};
```

- [ ] **Step 2: Verify build compiles**

```bash
npx ng build --configuration=development 2>&1 | tail -5
```

Expected: build succeeds with no errors

- [ ] **Step 3: Commit**

```bash
git add src/app/app.config.ts
git commit -m "feat(i18n): configure TranslateModule with HttpLoader in app.config"
```

---

### Task 3: Create `LanguageService`

**Files:**
- Create: `src/app/services/language.service.ts`
- Create: `src/app/services/language.service.spec.ts`

- [ ] **Step 1: Write the failing tests**

Create `src/app/services/language.service.spec.ts`:

```ts
import { TestBed } from '@angular/core/testing';
import { TranslateModule, TranslateService } from '@ngx-translate/core';
import { LanguageService } from './language.service';

describe('LanguageService', () => {
  let service: LanguageService;
  let translate: TranslateService;

  beforeEach(() => {
    localStorage.clear();
    TestBed.configureTestingModule({
      imports: [TranslateModule.forRoot()],
    });
    service = TestBed.inject(LanguageService);
    translate = TestBed.inject(TranslateService);
    translate.addLangs(['en', 'vi', 'zh', 'ja']);
    translate.setTranslation('en', {});
    translate.setTranslation('vi', {});
    translate.setTranslation('zh', {});
    translate.setTranslation('ja', {});
  });

  it('should be created', () => {
    expect(service).toBeTruthy();
  });

  describe('init()', () => {
    it('uses persisted language from localStorage', () => {
      localStorage.setItem('pickleball-lang', 'vi');
      service.init();
      expect(service.getCurrentLang()).toBe('vi');
    });

    it('falls back to "en" for unsupported browser locale', () => {
      spyOnProperty(navigator, 'language', 'get').and.returnValue('fr-FR');
      service.init();
      expect(service.getCurrentLang()).toBe('en');
    });

    it('maps "vi-VN" browser locale to "vi"', () => {
      spyOnProperty(navigator, 'language', 'get').and.returnValue('vi-VN');
      service.init();
      expect(service.getCurrentLang()).toBe('vi');
    });

    it('maps "zh-TW" browser locale to "zh"', () => {
      spyOnProperty(navigator, 'language', 'get').and.returnValue('zh-TW');
      service.init();
      expect(service.getCurrentLang()).toBe('zh');
    });

    it('maps "ja-JP" browser locale to "ja"', () => {
      spyOnProperty(navigator, 'language', 'get').and.returnValue('ja-JP');
      service.init();
      expect(service.getCurrentLang()).toBe('ja');
    });
  });

  describe('setLanguage()', () => {
    it('saves language to localStorage', () => {
      service.setLanguage('zh');
      expect(localStorage.getItem('pickleball-lang')).toBe('zh');
    });

    it('updates current language', () => {
      service.setLanguage('ja');
      expect(service.getCurrentLang()).toBe('ja');
    });
  });
});
```

- [ ] **Step 2: Run tests to verify they fail**

```bash
npx ng test --include="src/app/services/language.service.spec.ts" --watch=false
```

Expected: FAIL — `LanguageService` not found

- [ ] **Step 3: Create `src/app/services/language.service.ts`**

```ts
import { Injectable, inject } from '@angular/core';
import { TranslateService } from '@ngx-translate/core';

@Injectable({ providedIn: 'root' })
export class LanguageService {
  private readonly STORAGE_KEY = 'pickleball-lang';
  private readonly SUPPORTED = ['en', 'vi', 'zh', 'ja'];
  private readonly DEFAULT = 'en';

  private readonly translate = inject(TranslateService);

  init(): void {
    const persisted = localStorage.getItem(this.STORAGE_KEY);
    const lang = persisted && this.SUPPORTED.includes(persisted)
      ? persisted
      : this.detectBrowserLang();
    this.translate.use(lang);
  }

  setLanguage(lang: string): void {
    localStorage.setItem(this.STORAGE_KEY, lang);
    this.translate.use(lang);
  }

  getCurrentLang(): string {
    return this.translate.currentLang ?? this.DEFAULT;
  }

  private detectBrowserLang(): string {
    const browserLang = navigator.language?.split('-')[0] ?? '';
    return this.SUPPORTED.includes(browserLang) ? browserLang : this.DEFAULT;
  }
}
```

- [ ] **Step 4: Run tests to verify they pass**

```bash
npx ng test --include="src/app/services/language.service.spec.ts" --watch=false
```

Expected: All 7 tests pass

- [ ] **Step 5: Commit**

```bash
git add src/app/services/language.service.ts src/app/services/language.service.spec.ts
git commit -m "feat(i18n): add LanguageService with browser locale detection and localStorage persistence"
```

---

### Task 4: Create `AboutDialog` with version and language selector

**Files:**
- Create: `src/app/about-dialog/about-dialog.ts`
- Create: `src/app/about-dialog/about-dialog.html`
- Modify: `src/environments/environment.ts` (create if not exists)

- [ ] **Step 1: Check if `src/environments/` exists**

```bash
ls src/environments/ 2>/dev/null || echo "no environments dir"
```

If it does not exist, create the directory and the file. If it exists, read the current content first.

- [ ] **Step 2: Create or update `src/environments/environment.ts`**

```ts
import packageJson from '../../package.json';

export const environment = {
  production: false,
  appVersion: packageJson.version,
};
```

- [ ] **Step 3: Update `tsconfig.app.json` to allow JSON imports**

Open `tsconfig.app.json` and add `"resolveJsonModule": true` to `compilerOptions`:

```json
{
  "extends": "../tsconfig.json",
  "compilerOptions": {
    "outDir": "../out-tsc/app",
    "types": [],
    "resolveJsonModule": true
  },
  "files": [
    "main.ts"
  ],
  "include": [
    "**/*.d.ts"
  ]
}
```

- [ ] **Step 4: Create `src/app/about-dialog/about-dialog.ts`**

```ts
import { Component, inject } from '@angular/core';
import { CommonModule } from '@angular/common';
import { MatButtonModule } from '@angular/material/button';
import { MatDialogModule, MatDialogRef } from '@angular/material/dialog';
import { MatSelectModule } from '@angular/material/select';
import { MatFormFieldModule } from '@angular/material/form-field';
import { FormsModule } from '@angular/forms';
import { TranslateModule } from '@ngx-translate/core';
import { LanguageService } from '../services/language.service';
import { environment } from '../../environments/environment';

export const LANGUAGES = [
  { code: 'en', label: 'English' },
  { code: 'vi', label: 'Tiếng Việt' },
  { code: 'zh', label: '中文' },
  { code: 'ja', label: '日本語' },
];

@Component({
  selector: 'app-about-dialog',
  standalone: true,
  imports: [CommonModule, MatButtonModule, MatDialogModule, MatSelectModule, MatFormFieldModule, FormsModule, TranslateModule],
  templateUrl: './about-dialog.html',
})
export class AboutDialog {
  private readonly dialogRef = inject(MatDialogRef<AboutDialog>);
  private readonly languageService = inject(LanguageService);

  readonly languages = LANGUAGES;
  readonly version = environment.appVersion;
  selectedLang = this.languageService.getCurrentLang();

  onLangChange(lang: string): void {
    this.languageService.setLanguage(lang);
  }

  close(): void {
    this.dialogRef.close();
  }
}
```

- [ ] **Step 5: Create `src/app/about-dialog/about-dialog.html`**

```html
<h2 mat-dialog-title>{{ 'about.title' | translate }}</h2>
<mat-dialog-content style="min-width:280px;">
  <div style="margin-bottom:16px;">
    <div style="font-size:18px; font-weight:bold; color:#52b788; margin-bottom:4px;">
      🏓 Pickleball Round Robin
    </div>
    <div style="color:#aaa; font-size:13px;">{{ 'about.version' | translate: { version: version } }}</div>
  </div>

  <mat-form-field appearance="outline" style="width:100%;">
    <mat-label>{{ 'about.language_label' | translate }}</mat-label>
    <mat-select [(ngModel)]="selectedLang" (ngModelChange)="onLangChange($event)">
      @for (lang of languages; track lang.code) {
        <mat-option [value]="lang.code">{{ lang.label }}</mat-option>
      }
    </mat-select>
  </mat-form-field>
</mat-dialog-content>
<mat-dialog-actions align="end">
  <button mat-button (click)="close()">{{ 'about.close' | translate }}</button>
</mat-dialog-actions>
```

- [ ] **Step 6: Verify build compiles**

```bash
npx ng build --configuration=development 2>&1 | tail -5
```

Expected: build succeeds

- [ ] **Step 7: Commit**

```bash
git add src/app/about-dialog/ src/environments/ tsconfig.app.json
git commit -m "feat(about-dialog): add About dialog with version and language selector"
```

---

### Task 5: Wire `LanguageService` and `AboutDialog` into `App`

**Files:**
- Modify: `src/app/app.ts`
- Modify: `src/app/app.html`

- [ ] **Step 1: Update `src/app/app.ts`**

Replace the full file:

```ts
import { Component, OnInit, signal } from '@angular/core';
import { CommonModule } from '@angular/common';
import { MatTabsModule } from '@angular/material/tabs';
import { MatToolbarModule } from '@angular/material/toolbar';
import { MatButtonModule } from '@angular/material/button';
import { MatIconModule } from '@angular/material/icon';
import { MatBottomSheet, MatBottomSheetModule } from '@angular/material/bottom-sheet';
import { MatDialog, MatDialogModule } from '@angular/material/dialog';
import { TranslateModule } from '@ngx-translate/core';
import { SessionService } from './services/session.service';
import { LanguageService } from './services/language.service';
import { PlayersTab } from './players-tab/players-tab';
import { ScheduleTab } from './schedule-tab/schedule-tab';
import { ScoresTab } from './scores-tab/scores-tab';
import { LeaderboardTab } from './leaderboard-tab/leaderboard-tab';
import { SessionDrawer, SessionDrawerData, SessionDrawerResult } from './session-drawer/session-drawer';
import { AboutDialog } from './about-dialog/about-dialog';

@Component({
  selector: 'app-root',
  standalone: true,
  imports: [
    CommonModule, TranslateModule,
    MatTabsModule, MatToolbarModule, MatButtonModule, MatIconModule,
    MatBottomSheetModule, MatDialogModule,
    PlayersTab, ScheduleTab, ScoresTab, LeaderboardTab,
  ],
  templateUrl: './app.html',
  styleUrls: ['./app.scss'],
})
export class App implements OnInit {
  selectedDate = signal<string>('');
  selectedSessionNumber = signal<number>(1);
  readonly isReadOnly = signal<boolean>(false);

  constructor(
    readonly sessionService: SessionService,
    private readonly bottomSheet: MatBottomSheet,
    private readonly dialog: MatDialog,
    private readonly languageService: LanguageService,
  ) {
    this.languageService.init();
  }

  ngOnInit(): void {
    this.loadFromHash();
    window.addEventListener('hashchange', () => this.loadFromHash());
  }

  private loadFromHash(): void {
    const hash = window.location.hash.slice(1);
    if (hash) {
      const shared = this.sessionService.decodeSessionFromHash(hash);
      if (shared) {
        this.sessionService.loadSharedSession(shared);
        this.isReadOnly.set(true);
        return;
      }
    }
    this.sessionService.migrateOldKeys();
    const today = this.sessionService.todayDate();
    this.sessionService.initSession(today, 1);
    this.selectedDate.set(today);
    this.selectedSessionNumber.set(1);
  }

  openSessionDrawer(): void {
    const ref = this.bottomSheet.open(SessionDrawer, {
      data: {
        currentDate: this.selectedDate(),
        currentSessionNumber: this.selectedSessionNumber(),
      } as SessionDrawerData,
    });
    ref.afterDismissed().subscribe((result?: SessionDrawerResult) => {
      if (!result) return;
      if (result.deleted) {
        this.onSessionDeleted(result.date, result.sessionNumber);
      } else {
        this.onSessionChange(result.date, result.sessionNumber);
      }
    });
  }

  onSessionChange(date: string, sessionNumber: number): void {
    this.selectedDate.set(date);
    this.selectedSessionNumber.set(sessionNumber);
    this.sessionService.initSession(date, sessionNumber);
  }

  onSessionDeleted(date: string, deletedNumber: number): void {
    const remaining = this.sessionService.getSavedSessionsForDate(date);
    const next = remaining.length > 0 ? remaining[0] : 1;
    if (remaining.length === 0) {
      this.sessionService.initSession(date, 1);
    }
    this.onSessionChange(date, next);
  }

  openAboutDialog(): void {
    this.dialog.open(AboutDialog, { width: '320px' });
  }

  isToday(): boolean {
    return this.selectedDate() === this.sessionService.todayDate();
  }
}
```

- [ ] **Step 2: Update `src/app/app.html`**

Replace the full file — adds ℹ️ icon, removes "S" prefix from chip, uses `translate` pipe for tab labels:

```html
<mat-toolbar color="primary" style="background:#1a1a2e; position:sticky; top:0; z-index:10;">
  <span style="font-weight:bold; color:#52b788; font-size:18px;">🏓 Pickleball Round Robin</span>
  <span style="flex:1"></span>

  @if (!isReadOnly()) {
    <button
      mat-button
      (click)="openSessionDrawer()"
      style="color:#89b4fa; font-size:13px; border:1px solid #444; border-radius:16px; padding:4px 14px; line-height:1.5;"
    >
      📅 {{ selectedDate() | date:'MMM d' }} | {{ selectedSessionNumber() }} ▾
    </button>
  } @else {
    <span style="font-size:14px; color:#89b4fa;">{{ 'leaderboard.view_only' | translate }}</span>
  }

  <button mat-icon-button (click)="openAboutDialog()" style="color:#aaa; margin-left:4px;"
          aria-label="About">
    <mat-icon>info_outline</mat-icon>
  </button>
</mat-toolbar>

@if (isReadOnly()) {
  <div style="text-align:center; padding:10px; background:#1a2a3a; color:#89b4fa; font-size:14px;">
    {{ 'leaderboard.shared_readonly' | translate }}
  </div>
}

<mat-tab-group mat-stretch-tabs="false" animationDuration="200ms" style="margin-top:4px;">
  <mat-tab [label]="'players.title' | translate">
    <app-players-tab [readOnly]="isReadOnly()" />
  </mat-tab>
  <mat-tab [label]="'schedule.title' | translate">
    <app-schedule-tab />
  </mat-tab>
  <mat-tab [label]="'scores.title' | translate">
    <app-scores-tab [readOnly]="isReadOnly()" />
  </mat-tab>
  <mat-tab [label]="'leaderboard.title' | translate">
    <app-leaderboard-tab [readOnly]="isReadOnly()" />
  </mat-tab>
</mat-tab-group>
```

- [ ] **Step 3: Run unit tests**

```bash
npx ng test --watch=false
```

Expected: All tests pass

- [ ] **Step 4: Commit**

```bash
git add src/app/app.ts src/app/app.html
git commit -m "feat(app): wire LanguageService init, AboutDialog, and i18n toolbar chip"
```

---

### Task 6: Apply `translate` pipe to all tab templates

**Files:**
- Modify: `src/app/players-tab/players-tab.html`
- Modify: `src/app/players-tab/players-tab.ts`
- Modify: `src/app/schedule-tab/schedule-tab.html`
- Modify: `src/app/schedule-tab/schedule-tab.ts`
- Modify: `src/app/scores-tab/scores-tab.html`
- Modify: `src/app/leaderboard-tab/leaderboard-tab.html`
- Modify: `src/app/session-drawer/session-drawer.html`
- Modify: `src/app/session-drawer/session-drawer.ts`
- Modify: `src/app/confirm-dialog/confirm-dialog.html`

Note: If Task 2 of the session-reset plan has not been completed yet, `ConfirmDialog` may not exist. Complete that plan first.

- [ ] **Step 1: Update `src/app/players-tab/players-tab.ts`** — add `TranslateModule` and `TranslateService`

Add to imports array: `TranslateModule`
Add to component imports: `import { TranslateModule, TranslateService } from '@ngx-translate/core';`
Inject `TranslateService`: `private readonly translate = inject(TranslateService);`

Update `removePlayer()` snackbar message:
```ts
removePlayer(id: string): void {
  if (this.scheduleGenerated()) {
    this.snackBar.open(this.translate.instant('players.roster_locked_snack'), 'OK', { duration: 3000 });
    return;
  }
  this.sessionService.removePlayer(id);
}
```

Update `generateSchedule()` confirm message:
```ts
generateSchedule(): void {
  const session = this.sessionService.activeSession();
  if (!session || !this.canGenerate()) return;
  const hasExisting = session.rounds.length > 0;
  if (hasExisting) {
    const ok = confirm(this.translate.instant('confirm_dialog.regenerate_message'));
    if (!ok) return;
  }
  const rounds = this.scheduleService.generateRounds(session.players);
  this.sessionService.setRounds(rounds);
}
```

- [ ] **Step 2: Update `src/app/players-tab/players-tab.html`** — replace all hardcoded strings

Replace the full file:

```html
<div style="max-width:500px; margin:0 auto;">

  <div style="display:flex; align-items:center; justify-content:space-between; margin-bottom:16px;">
    <h2 style="margin:0; color:#52b788;">{{ 'players.title' | translate }}</h2>
    <span class="status-badge" [class]="players().length >= 8 ? 'badge-completed' : 'badge-upcoming'">
      {{ players().length }} / 11
    </span>
  </div>

  @if (!readOnly) {
    <div style="display:flex; gap:8px; margin-bottom:16px; align-items:flex-start;">
      <mat-form-field appearance="outline" style="flex:1;">
        <mat-label>{{ 'players.add_placeholder' | translate }}</mat-label>
        <input #nameInput matInput [ngModel]="newName()" (ngModelChange)="newName.set($event)" (keydown)="onKeydown($event, nameInput)" maxlength="30" />
      </mat-form-field>
      <button mat-raised-button color="primary" (click)="addPlayer()" [disabled]="!canAdd()"
              style="margin-top:4px;">
        {{ 'players.add_button' | translate }}
      </button>
    </div>
  }

  @if (players().length === 0) {
    <p class="text-muted" style="text-align:center;">{{ 'players.empty_state' | translate }}</p>
  }

  <mat-list>
    @for (player of players(); track player.id; let i = $index) {
      <mat-list-item style="background:#2a2a3e; border-radius:8px; margin-bottom:6px;">
        <span matListItemTitle style="font-size:18px;">{{ i + 1 }}. {{ player.name }}</span>
        @if (!readOnly) {
          <button matListItemMeta mat-icon-button color="warn" (click)="removePlayer(player.id)"
                  [disabled]="scheduleGenerated()"
                  [matTooltip]="scheduleGenerated() ? ('players.delete_tooltip' | translate) : ''"
                  [attr.aria-label]="'Remove ' + player.name">
            <mat-icon>close</mat-icon>
          </button>
        }
      </mat-list-item>
    }
  </mat-list>

  @if (!readOnly && canGenerate()) {
    <button mat-raised-button color="primary" style="width:100%; margin-top:16px;"
            (click)="generateSchedule()">
      {{ 'players.generate_button' | translate: { count: players().length } }}
    </button>
  }

  @if (!readOnly && players().length > 0 && players().length < 8) {
    <p class="text-muted" style="text-align:center; margin-top:8px;">
      {{ 'players.add_more' | translate: { count: 8 - players().length } }}
    </p>
  }

</div>
```

- [ ] **Step 3: Update `src/app/schedule-tab/schedule-tab.html`** — add `TranslateModule` import to `schedule-tab.ts` and replace strings in template

Add `TranslateModule` to `schedule-tab.ts` imports array.

Replace the schedule tab html:

```html
<div style="max-width:500px; margin:0 auto;">
  <h2 style="color:#52b788;">{{ 'schedule.title' | translate }}</h2>

  @if (rounds().length === 0) {
    <p class="text-muted" style="text-align:center;">
      {{ 'schedule.empty_state' | translate }}
    </p>
  }

  @for (round of rounds(); track round.roundNumber; let i = $index) {
    <mat-card class="round-card" [class]="roundStatus(i)"
              [attr.aria-label]="('schedule.round' | translate: { number: round.roundNumber }) + ', ' + roundStatus(i)">
      <mat-card-header>
        <mat-card-title style="font-size:16px; font-weight:bold;">
          {{ 'schedule.round' | translate: { number: round.roundNumber } }}
        </mat-card-title>
        <span style="flex:1;"></span>
        <span class="status-badge" [class]="'badge-' + roundStatus(i)">
          {{ roundStatus(i) === 'active' ? 'NOW' : (roundStatus(i) | uppercase) }}
        </span>
      </mat-card-header>

      <mat-card-content style="margin-top:12px;">
        @for (court of round.courts; track court.courtName) {
          <div style="margin-bottom:10px;">
            <div class="text-muted text-small" style="margin-bottom:4px; font-weight:bold;">
              {{ court.courtName }}
            </div>
            <div style="display:flex; align-items:center; gap:8px; font-size:16px; flex-wrap:wrap;">
              <span>{{ playerName(court.team1[0]) }} &amp; {{ playerName(court.team1[1]) }}</span>
              @if (court.score) {
                <span class="score-display">
                  {{ court.score.team1 }}–{{ court.score.team2 }}
                </span>
              } @else {
                <span class="text-muted">vs</span>
              }
              <span>{{ playerName(court.team2[0]) }} &amp; {{ playerName(court.team2[1]) }}</span>
            </div>
          </div>
        }

        @if (round.sittingOut.length > 0) {
          <div class="text-muted text-small" style="margin-top:6px;">
            {{ 'schedule.sitting_out' | translate: { names: sittingOutNames(round.sittingOut) } }}
          </div>
        }
      </mat-card-content>
    </mat-card>
  }

  @if (rounds().length > 0) {
    <button mat-stroked-button color="warn" style="width:100%; margin-top:16px;"
            (click)="openRegenerateDialog()">
      {{ 'schedule.regenerate_button' | translate }}
    </button>
  }
</div>
```

- [ ] **Step 4: Update `src/app/scores-tab/scores-tab.html`** — translate title and empty state

Add `TranslateModule` to `scores-tab.ts` imports array.

Change line 2 (`<h2>`) to:
```html
<h2 style="color:#52b788;">{{ 'scores.title' | translate }}</h2>
```

Change the empty state `<p>` to:
```html
<p class="text-muted" style="text-align:center;">{{ 'scores.empty_state' | translate }}</p>
```

- [ ] **Step 5: Update `src/app/leaderboard-tab/leaderboard-tab.html`** — translate all strings

Add `TranslateModule` to `leaderboard-tab.ts` imports array.

Replace the full leaderboard html:

```html
<div style="max-width:500px; margin:0 auto;">
  <div style="display:flex; align-items:center; justify-content:space-between; margin-bottom:16px;">
    <h2 style="margin:0; color:#52b788;">{{ 'leaderboard.title' | translate }}</h2>
    @if (!readOnly) {
      <button mat-stroked-button color="primary" (click)="openShare()">
        {{ 'leaderboard.share_button' | translate }}
      </button>
    }
  </div>

  <mat-button-toggle-group [value]="sortBy()" (change)="sortBy.set($event.value)"
                           aria-label="Sort leaderboard by"
                           style="margin-bottom:16px; width:100%;">
    <mat-button-toggle value="wins" style="flex:1;">{{ 'leaderboard.sort_wins' | translate }}</mat-button-toggle>
    <mat-button-toggle value="points" style="flex:1;">{{ 'leaderboard.sort_points' | translate }}</mat-button-toggle>
  </mat-button-toggle-group>

  @if (!hasScores()) {
    <p class="text-muted" style="text-align:center;">{{ 'leaderboard.no_scores' | translate }}</p>
  } @else {
    @for (entry of stats(); track entry.player.id; let i = $index) {
      <mat-card class="round-card" style="margin-bottom:8px;" role="row"
                [attr.aria-label]="'Rank ' + (i+1) + ': ' + entry.player.name + ', ' + entry.wins + ' wins, ' + entry.totalPoints + ' points'">
        <mat-card-content style="padding:12px !important;">
          <div style="display:flex; align-items:center; gap:12px;">
            <span style="font-size:22px; width:32px; text-align:center;" aria-hidden="true">
              {{ medal(i) || (i + 1) }}
            </span>
            <div style="flex:1;">
              <div class="player-name">{{ entry.player.name }}</div>
              <div class="player-stat text-muted">
                {{ 'leaderboard.stat_line' | translate: { wins: entry.wins, points: entry.totalPoints, games: entry.gamesPlayed } }}
              </div>
            </div>
            <span class="status-badge badge-completed">
              {{ sortBy() === 'wins' ? entry.wins + 'W' : entry.totalPoints + 'pts' }}
            </span>
          </div>
        </mat-card-content>
      </mat-card>
    }
  }

  @if (!readOnly) {
    <button mat-stroked-button color="warn" style="width:100%; margin-top:24px;" (click)="openResetDialog()">
      {{ 'leaderboard.reset_button' | translate }}
    </button>
  }
</div>
```

- [ ] **Step 6: Update `src/app/session-drawer/session-drawer.ts`** — inject `TranslateService` for dialog title

Add import: `import { TranslateService } from '@ngx-translate/core';`
Add `TranslateModule` to component imports array.
Inject: `private readonly translate = inject(TranslateService);`

Update `openDeleteDialog()` to use translated title:
```ts
openDeleteDialog(sessionNumber: number): void {
  const ref = this.dialog.open(ConfirmDialog, {
    data: {
      title: this.translate.instant('session_drawer.delete_confirm_title', { number: sessionNumber }),
      message: this.translate.instant('session_drawer.delete_confirm_message'),
      actions: [{ label: this.translate.instant('confirm_dialog.delete'), value: 'delete', color: 'warn' as const }],
    } as ConfirmDialogData,
  });
  ref.afterClosed().subscribe(value => {
    if (value !== 'delete') return;
    this.sessionService.clearSession(this.selectedDate(), sessionNumber);
    this.sheetRef.dismiss({ date: this.selectedDate(), sessionNumber, deleted: true });
  });
}
```

- [ ] **Step 7: Update `src/app/session-drawer/session-drawer.html`** — translate strings

Replace the full file:

```html
<div style="padding:16px; max-width:400px; margin:0 auto;">
  <h3 style="margin:0 0 16px; color:#52b788; font-size:16px; font-weight:600;">
    {{ 'session_drawer.title' | translate }}
  </h3>

  <div style="margin-bottom:16px;">
    <label style="display:block; font-size:12px; color:#aaa; margin-bottom:6px; text-transform:uppercase; letter-spacing:0.5px;">
      {{ 'session_drawer.date_label' | translate }}
    </label>
    <input
      type="date"
      [ngModel]="selectedDate()"
      (ngModelChange)="onDateChange($event)"
      style="width:100%; background:#2a2a4e; color:#e0e0e0; border:1px solid #52b788; border-radius:4px; padding:8px 10px; font-size:14px; box-sizing:border-box;"
    />
  </div>

  <div style="margin-bottom:12px;">
    @for (n of sessions(); track n) {
      <div style="display:flex; align-items:center; gap:8px; margin-bottom:8px;">
        <button
          mat-stroked-button
          (click)="selectSession(n)"
          style="flex:1;"
          [style.background]="isCurrentSession(n) ? '#52b788' : 'transparent'"
          [style.color]="isCurrentSession(n) ? '#1a1a2e' : '#e0e0e0'"
          [style.border-color]="isCurrentSession(n) ? '#52b788' : '#444'"
        >
          Session {{ n }}
          @if (isCurrentSession(n)) {
            <span style="margin-left:8px; font-size:11px; opacity:0.8;">
              ✓ {{ 'session_drawer.active_label' | translate }}
            </span>
          }
        </button>
        <button mat-icon-button color="warn" (click)="openDeleteDialog(n)"
                [attr.aria-label]="'Delete session ' + n">
          <mat-icon>delete</mat-icon>
        </button>
      </div>
    }
    @if (sessions().length === 0) {
      <p style="color:#aaa; font-size:13px; text-align:center; margin:12px 0;">
        {{ 'session_drawer.empty_state' | translate }}
      </p>
    }
  </div>

  <button
    mat-stroked-button
    (click)="addNewSession()"
    style="width:100%; border-style:dashed; color:#52b788; border-color:#52b788;"
  >
    {{ 'session_drawer.new_session' | translate }}
  </button>
</div>
```

- [ ] **Step 8: Update `src/app/confirm-dialog/confirm-dialog.html`** — translate Cancel button

Replace:
```html
<button mat-stroked-button (click)="cancel()">Cancel</button>
```
With:
```html
<button mat-stroked-button (click)="cancel()">{{ 'confirm_dialog.cancel' | translate }}</button>
```

Also add `TranslateModule` to `confirm-dialog.ts` imports array.

- [ ] **Step 9: Run unit tests**

```bash
npx ng test --watch=false
```

Expected: All tests pass

- [ ] **Step 10: Commit**

```bash
git add src/app/players-tab/ src/app/schedule-tab/ src/app/scores-tab/ src/app/leaderboard-tab/ src/app/session-drawer/ src/app/confirm-dialog/
git commit -m "feat(i18n): apply translate pipe to all tab and drawer templates"
```

---

### Task 7: E2E tests for i18n

**Files:**
- Create: `tests/i18n/i18n.spec.ts`

- [ ] **Step 1: Create `tests/i18n/i18n.spec.ts`**

```ts
import { test, expect } from '@playwright/test';
import { freshState } from '../helpers';

const BASE_URL = process.env['BASE_URL'] ?? 'http://localhost:4200/roundrobin';

test.describe('i18n — Language Switching', () => {

  test('9.1 Default language is English', async ({ page }) => {
    await freshState(page);
    await expect(page.getByRole('tab', { name: 'Players' })).toBeVisible();
    await expect(page.getByRole('tab', { name: 'Schedule' })).toBeVisible();
  });

  test('9.2 Switching to Vietnamese changes UI labels', async ({ page }) => {
    await freshState(page);
    await page.getByRole('button', { name: 'About' }).click();
    await page.getByRole('dialog').waitFor({ state: 'visible' });
    await page.getByRole('combobox').selectOption('vi');
    await page.getByRole('button', { name: /Đóng|Close/i }).click();
    await expect(page.getByRole('tab', { name: 'Người chơi' })).toBeVisible();
    await expect(page.getByRole('tab', { name: 'Lịch thi đấu' })).toBeVisible();
  });

  test('9.3 Language persists across page reload', async ({ page }) => {
    await freshState(page);
    await page.getByRole('button', { name: 'About' }).click();
    await page.getByRole('dialog').waitFor({ state: 'visible' });
    await page.getByRole('combobox').selectOption('zh');
    await page.getByRole('button', { name: /关闭|Close/i }).click();
    await page.reload();
    await expect(page.getByRole('tab', { name: '球员' })).toBeVisible();
  });

  test('9.4 Session chip shows no "S" prefix in any language', async ({ page }) => {
    await freshState(page);
    const chip = page.getByRole('button', { name: /📅/ });
    await expect(chip).not.toContainText('S1');
    await expect(chip).toContainText('| 1');
  });

  test('9.5 Switching to Japanese changes UI labels', async ({ page }) => {
    await freshState(page);
    await page.getByRole('button', { name: 'About' }).click();
    await page.getByRole('dialog').waitFor({ state: 'visible' });
    await page.getByRole('combobox').selectOption('ja');
    await page.getByRole('button', { name: /閉じる|Close/i }).click();
    await expect(page.getByRole('tab', { name: 'プレイヤー' })).toBeVisible();
  });
});
```

- [ ] **Step 2: Run E2E tests**

```bash
npx playwright test tests/i18n/i18n.spec.ts --project=chromium
```

Expected: All 5 tests pass

- [ ] **Step 3: Run full test suite**

```bash
npx ng test --watch=false && npx playwright test --project=chromium
```

Expected: All unit tests and E2E tests pass

- [ ] **Step 4: Commit**

```bash
git add tests/i18n/i18n.spec.ts
git commit -m "test(e2e): add i18n language switching E2E tests"
```
